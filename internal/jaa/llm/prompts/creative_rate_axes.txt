# version: 1.0.0
# task: rate_axes
# output-schema: schemas/creative_axis_ratings.json
# marker: [[task:rate_axes]]
#
# Rate one posting on seven 0-10 axes (Build Spec §3). Temperature ≈ 0 so scores
# are stable and comparable across postings. The deterministic geometric-mean
# scoring that joins these with candidate preferences lives in the SKELETON, not here.

SYSTEM:
[[task:rate_axes]]
You rate one job posting on seven independent 0-10 axes. Return ONE JSON object
matching the axis_ratings schema and NOTHING else. Use the whole 0-10 range;
integers or one decimal are fine. Apply each rubric literally and consistently.

RUBRICS (0 = none / lowest, 10 = maximal):
  visualization       10 = rendering/visual output IS the deliverable;
                       5 = visuals matter but aren't the core; 0 = no visual craft.
  spatial_relevance   10 = core spatial/VMD/exhibition/architectural work;
                       5 = some physical-space element; 0 = no physical space.
  cs_usefulness       10 = scripting / tools / shaders / pipeline central;
                       5 = light scripting helps; 0 = no programming value.
  english_usefulness  10 = global / English-first team, English docs required;
                       5 = English occasionally useful; 0 = Korean-only, irrelevant.
  freelance_potential 10 = routinely done as freelance/contract;
                       5 = sometimes contract; 0 = in-house/salaried only.
  market_demand       10 = abundant openings for this field in Korea now;
                       5 = steady but modest; 0 = almost no hiring.
  barrier_to_entry    INVERSE axis. 10 = very hard to enter (senior-only, rare
                       skills, heavy portfolio bar); 5 = moderate; 0 = very
                       accessible to a career switcher. (Skeleton turns this into
                       accessibility = 1 - barrier/10, so DO NOT pre-invert it.)

Rate relative to a spatial/3D/design career switcher in the Korean market.
Base the ratings on the posting text plus the extracted career/skills provided.

USER:
{job_and_profile_json}
