# Market Aligner reconstruction report

**Date:** 2026-08-01  
**Repository:** `/Users/admin/Projects/market-aligner`  
**Branch:** `codex/reconciliation-20260801`  
**Source policy:** all audited candidates remained read-only; no destructive move or tombstone
was performed.

## Outcome

A new canonical Git lineage now contains one generic `market-aligner` package. The same CLI and
service code ran two real private profiles and one synthetic new-user profile through distinct
opaque `profile_id` values. Their files live under an external data home, not the repository.
The acceptance state contains three assessments for three profiles and exposes only
`fit_status=uncalibrated`.

A byte-identical, integrity-checked private copy of the strongest vacancy database was migrated
to the external data home: 10,437 postings, 632 normalised rows, 632 historical score rows and 39
source-state records. The source and target SHA-256 are both
`d0aada412b37c959a598cd03a2b67db8c3e2ef93e03b7c5256a49ad8f33aa5df`.

## Provenance and disposition

| Subsystem | Strongest audited source | Decision and proof | Canonical target |
|---|---|---|---|
| Product identity/Git | Reconciliation audit; legacy name only | Adopted into a standalone `codex/` branch; no personal package metadata | repository root |
| Profile validation | Korea evidence-led profiler | Selectively adopted numeric/status/secret/reference validation; person defaults removed | `profiler/` |
| Private profiles | Korea private sources | Imported externally under opaque IDs; one evidence-led ledger has 27 items; the guided legacy profile retains zero ledger items and explicit unknowns | external data home |
| Collectors/adapters | Korea collector and 36 adapter modules | Adopted 34 registered source adapters; 301-record regression proves no result cap | `collectors/` |
| Scrapling | Korea client/worker, runtime 0.4.11 | Full fetch/session/parse/spider/call/capabilities protocol certified, including static/dynamic/stealth and typed refs | `collectors/scrapling_*` |
| Fetch control | Korea `career_automation/fetching.py` | Adopted immutable policy, attempt, fingerprint and relocation receipts; original tests ported | `collectors/fetch_control.py` |
| Vacancy persistence | Korea scraper SQLite | Adopted WAL persistence, source state, pending resume and legacy import | `state/vacancies.py` |
| Legacy resume/import | Legacy `market-aligner/skeleton/run.py` | Adopted recursive config extension, cycle rejection, multi-root JSON/JSONL loading and key deduplication | `config_loader.py`, `state/importers.py` |
| Normalisation/dedup/viability | Korea deterministic viability gate | Generalised shell extraction, URL/company/title/location identity, representative selection, expiry/access/completeness; market/person filters removed | `normalisation/`, `assessment/viability.py` |
| Hard eligibility | Reconciliation boundary over Korea/JAA concepts | New deterministic explicit-fact policy; missing facts return review rather than invented answers | `assessment/eligibility.py` |
| Semantic work boundary | Korea schemas plus audit contracts | Versioned extraction/alignment schemas; raw/profile evidence hashes and LLM receipts enforced; unknown evidence IDs rejected | `llm/` |
| Fit/opportunity arithmetic | Korea scoring engine | Power-mean arithmetic and aggregation adopted generically; parameter hash emitted; only uncalibrated status exists | `assessment/scoring.py` |
| Opportunity/research gate | Korea control-plane database/engine | Adapted to composite `profile_id/job_key`; SQLite trigger forbids research before opportunity pass | `assessment/opportunity.py`, `research/` |
| Research workers/dossiers | Korea queue/lease/dossier patterns | Leases, retries, source-cited claims and mismatched-task rejection tested | `research/` |
| Migrations | Korea migration runner | Table namespace changed; checksum, rollback, ordering and idempotency tests pass | `state/migrations.py` |
| Observability/workers | Korea observability | Trace/span/event/outbox leases, retry and dead-message behavior retained with original tests | `state/observability.py` |
| Reports | Korea reporter | Requirements frequency logic adapted to dependency-free ranked CSVs and interactive HTML scatter; profile scoping and uncalibrated label tested | `reporting/` |
| Local service/API | Missing coherent predecessor | Implemented a small in-process service reused by CLI; no distributed platform | `service/` |
| JAA boundary | Local JAA plans at parent commit `ab4b611…` | Provisional v0 handoff/events adopted; submission authorization and receipt events have hard hash gates | `applications/` |
| Gutua JAA implementation | Active protected Gutua worktree | Deferred; no code imported or overwritten | sealed import pending |
| External orchestrator | Operator boundary | Protocol-only capability-scoped adapter; infrastructure not absorbed | `integrations/orchestrator.py` |

Every adopted source file/group is bound to an exact SHA-256 in `ledger.jsonl`; root source
manifests remain in `source-manifests.json`.

## Adopted

- canonical Git lineage, package identity, privacy boundary and source manifests;
- generic opaque profiles, evidence ledger, private store and legacy importers;
- all audited collector adapters, uncapped collector and complete Scrapling sidecar;
- fetch control, vacancy SQLite, migrations, observability and resumability/multi-root import;
- generic normalisation, deduplication, viability, hard eligibility and deterministic scoring;
- semantic extraction/evidence-alignment schemas and receipt acceptance;
- profile-aware opportunity gate, research queue/worker/dossier contracts;
- ranked jobs, ranked requirements and interactive opportunity/fit reporting;
- local service/CLI and provisional JAA/orchestrator interfaces;
- original Korea fixtures and ported regression tests.

## Deferred

- final canonical evidence/fact/claim reconciliation with the sealed Gutua JAA-02 contract;
- import of any Gutua JAA implementation, Playwright application flow or application state;
- concrete LLM provider/model/prompt routing and live semantic worker operation;
- concrete employer-research provider execution (the gated worker/provider seam is ready);
- fit calibration until operator labels and real outcomes satisfy the calibration gate;
- HTTP transport, remote concurrency/auth/storage, commercial tenancy/billing and deployment;
- unsupported-form automation and every automatic irreversible submission.

## Archived

No component was newly archived in this implementation pass. All audited predecessor directories,
databases, caches, reports and generated outputs remain preserved in place and hash-bound by the
read-only audit. Archive relocation is deliberately pending because the operator prohibited
destructive moves before adoption verification.

## Tombstoned

No source was tombstoned or deleted. The redundant `market-aligner-artiom` fork, Korea Job Scraper
product identity, person-named legacy contracts/defaults and legacy release ZIP are
**tombstone-pending**. They have no role in the new canonical package, but their existing source
directories remain untouched until a separately authorized deprecation pass creates archive and
tombstone receipts.

## Private acceptance evidence

External acceptance root:
`/Users/admin/Library/Application Support/market-aligner/reconciliation-20260801`

- three opaque profiles exist: two imported real profiles and one synthetic new user;
- the same `market-aligner assess --profile-id ...` command assessed all three;
- the assessment database reports `3` rows, `3` distinct profile IDs and only `uncalibrated` fit;
- profile and vacancy migration receipts are stored under the external
  `migration-receipts/` directory;
- no private profile, mapping, vacancy body, credential or generated output is tracked in Git or
  included in the wheel.

## Exact blockers

1. The current Gutua JAA HEAD, dirty state, full manifest and final certification receipts have not
   been sealed and transferred to the Mac. JAA import is therefore forbidden.
2. The final Gutua JAA-02 evidence/fact/claim schema may require a versioned translator from the
   current local evidence-ledger schema.
3. The guided private profile has no itemised evidence ledger and no measured market-readiness
   value. It runs truthfully with those fields unknown/zero, but cannot support evidence-backed
   application claims until reconciled.
4. The two historical variants of that guided profile have not yet received a field-level conflict
   reconciliation; the Korea variant was imported for execution without claiming it is final.
5. Fit is uncalibrated; adequate operator judgments and real application outcomes do not yet exist.
6. Concrete LLM and account-research providers remain unconfigured; only their validated worker
   boundaries are implemented.
7. No remote is configured for this repository, so daily push was impossible.
8. Tombstoning the redundant source fork and legacy product identities requires a separate
   authorized, hash-verified deprecation pass.

The final handoff message supplies the exact final commit hash and certification command/result.
