# JAA execution checkpoint

Date: 2026-08-04 KST

## Executive verdict

JAA is materially advanced but not finished or promoted. The exact Gutua source
and JAA-10 certification lineage are preserved, the Mac now has an explicit
certification profile, production-bounded ATS adapters exist for Recruitee,
Ashby and Personio, and executable local foundations now exist through JAA-16.

No current role in the bounded canary search passed every eligibility,
evidence, form and release gate. CloudCops reached the Personio adapter but was
parked because the approved evidence packet does not support several role
requirements and because its prepared artifacts were not genuine JAA-05 through
JAA-08 release objects. The next evidence-compatible candidate exposed
Cloudflare Turnstile. No applicant data was entered, no token was issued or
consumed, and no submit click occurred.

## Completed in this execution

1. Preserved source commit
   `974b41a566fd3fb95abc12827093ed681d87b31c` and tree
   `212fea4b7a4924dbb3f1f7ebc30445953ddf1d51` as the imported baseline.
2. Preserved the independent JAA-10 exact-source certification and its raw
   evidence hash
   `72b27f845cd54b7c9ba2e70485623a3cffbc1cee7636c292fd50e0b0352f2a68`.
3. Imported the exact 92-file, 34,932,504-byte JAA-09 authority corpus and made
   its root explicit through `JAA_CERTIFIED_CORPUS_ROOT`.
4. Removed `the implicit home directory` defaults from three tracked shipping modules without
   weakening their fail-closed identity checks.
5. Added an exact Mac certification profile. It inventories 36 JAA-09/JAA-10
   files: 29 Mac-applicable files and seven Linux-bound suites. Four require
   the private namespace witness, two require Linux `CLOCK_BOOTTIME`, and one
   requires the frozen Linux Chromium render identity. It
   rejects broad skips, symlinks, hash drift, dirty source and unknown files.
6. Implemented production-bounded Recruitee, Ashby and Personio adapters with
   route/schema/artifact binding, JAA-08 token consumption immediately before
   the sole permitted click, durable one-use/indeterminate state and hash-only
   receipts. Decoded parks on hCaptcha and Vega parks on reCAPTCHA.
7. Implemented a real CloudCops pipeline bridge through the existing JAA-05,
   JAA-06, JAA-07 and JAA-08 stores and objects. The bridge is unarmed by
   default and issues no token when evidence or authority is insufficient.
8. Independently reviewed the earlier CloudCops CV and prepared manifest. The
   manifest was not an authentic JAA-07/JAA-08 release object and the CV was not
   mechanically derived from the approved evidence ledger. A content-addressed
   tombstone now prevents the historical prepared manifest from being armed.
9. Searched the bounded post-top-20 candidate set. No release-ready target was
   found; the result is recorded in the canary selection assessment.
10. Implemented JAA-12 source-backed JSON/EML/text status ingestion, censored
    silence and an append-only at-most-once follow-up-due ledger.
11. Implemented JAA-13 source-backed interview preparation, content-addressed
    debriefs and non-sendable follow-up drafts.
12. Implemented JAA-14 append-only predictions, outcomes, calibration and
    single-variable experiment evaluation without self-promotion.
13. Implemented JAA-15 deterministic, non-network adapter acquisition records
    and activation gates without crawling or activating adapters.
14. Implemented JAA-16 local SQLite supervision, scheduling, leases,
    backpressure, budgets, pause/resume, provider fallback, drills, local
    reports and backup/restore metadata without deployment or external sends.

## Verified results

- JAA-08 plus JAA-11 convergence: **637 passed**.
- JAA-11 through JAA-16 plus the certification profile, evidence sealer and
  runner: **932 passed**.
- Dirty-tree diagnostic of the 29 Mac-applicable JAA-09/JAA-10 files:
  **424 passed**, with only the intentional clean-checkout negative control
  failing before commit.
- Exact-corpus JAA-09 suite: **68 passed**.
- Generic distributable path-free gate: **1 passed**.
- Python compilation and `git diff --check`: passed.

These are implementation and compatibility results. They are not a live-canary
certificate, an external-operation certificate or a final JAA-00-through-JAA-16
product certificate.

## Precise remaining work

### Gate 1: portable certification evidence

Produce the final clean-worktree Mac certification receipt, exact manifests for
the transferred JAA-10 external-control evidence and exact retained-Linux
evidence verification. The profile already enforces the classification; the
remaining work is evidence packaging, clean-source execution and independent
review.

### Gate 2: a release-ready vacancy

Take a fresh frozen Market Aligner candidate at rank 21 or lower and require all
of the following simultaneously: current and eligible, no duplicate, truthful
approved evidence for every material answer and document claim, stable official
schema, and no CAPTCHA, MFA, login, account creation, payment or unknown legal
attestation. The bounded search found no such target, so this gate cannot be
completed losslessly by weakening requirements.

### Gate 3: one authentic JAA-11 release

For the first target that passes Gate 2, compile real JAA-05 through JAA-08
objects, pass an independent no-submit review, arm one hash-bound token, execute
at most one click, capture an official receipt and independently certify the
exact source/evidence set. Any uncertain click becomes permanently indeterminate
and is never retried.

### Gate 4: downstream promotion

JAA-12 through JAA-16 now have executable safe foundations. Promotion still
requires real post-application events and outcomes, locked holdout/live
evaluation, independent certification, activated adapters only after JAA-14
promotion, clean-machine unattended-operation evidence, rollback evidence and
installable release artifacts. No downstream slice may promote itself across an
unsatisfied upstream gate.

### Gate 5: final convergence

Run the applicable clean Mac profile and retained Linux evidence profile,
independently certify every promoted slice, publish the installation/upgrade/
rollback artifacts, and export the final Market Aligner contract: Market
Aligner selects and prepares; JAA validates, releases, executes and records.

## Lossless concurrency

The following may proceed concurrently because they write separate,
non-promoting boundaries:

- portable evidence packaging and retained-Linux manifest verification;
- fresh target discovery and read-only official-page capture;
- JAA-12/JAA-13 local ingestion and preparation against supplied evidence;
- JAA-15 non-activating adapter evidence acquisition;
- JAA-16 local drills, packaging and restore verification.

The following must remain sequential:

1. vacancy/evidence/form acceptance before JAA-05 through JAA-08 compilation;
2. independent no-submit review before token issuance;
3. token issuance before the sole click;
4. official receipt before JAA-11 certification;
5. real outcomes before JAA-14 promotion;
6. JAA-14 promotion before JAA-15 activation;
7. all upstream certificates before JAA-16 product release.
